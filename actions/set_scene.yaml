---
name: "set_scene"
runner_type: "python-script"
description: "Sets a scene in the defined rooms."
enabled: true
entry_point: "set_scene.py"
parameters:
  rooms:
    description: "List of rooms in which to set a given scene."
    type: "array"
    required: true
    position: 0
  scene:
    description: "Scene to set."
    type: "string"
    required: false
    position: 1
  color:
    description: "HSBK Color Value, i.e. (0, 0, 65535, 4000) or (hue, saturation, brightnees, temperature (K))." 
    type: "array"
    items:
      type: "integer"
    minItems: 4
    maxItems: 4
    required: false
    position: 2
  brightness:
    description: "Light brightness as a percent (0-100)%."
    type: "integer"
    required: false
    minimum: 0
    maximum: 100
    position: 3