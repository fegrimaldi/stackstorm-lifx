version: 1.0

description: Sets house to the daylight light scene.

input:
  - rooms: ["media_room", "office", "bedroom", "living_room"]
  - scene: "daylight"
  - power: true
output:
  - result: <% ctx().result %>

tasks:
  set_scene:
    action: lifx.set_scene
    input:
      rooms: <% ctx().rooms %>
      scene: <% ctx().scene %>
      power: <% ctx().power %>
    next:
      - when: <% succeeded() %>
        publish: 
          - result: <% result().result %>

     